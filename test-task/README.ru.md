# Тестовое задание: BrownEvents

> Read in English: [README.md](README.md)
>
> Русская версия — для удобства чтения. Тексты самих тасок и сайд-квестов — на английском,
> рабочем языке компании; issues в вашем репозитории тоже будут английскими.

Перед вами — браунфилд: работающее приложение для управления конференциями, которое два года никто не поддерживал. Ваша задача — разобраться в нём, стабилизировать его и достроить две фичи, работая в паре с консольным AI-агентом. Оценивается не только результат, но и *как* вы работаете с агентом — поэтому у задания есть правила трассировки и финальный отчёт.

**Срок: ориентировочно 14–20 дней** с момента получения доступа к репозиторию. Это ориентир, а не отсечка — можно потратить и больше, просто предупредите проверяющего.

## С чем вы работаете

BrownEvents — приложение для управления конференциями, написанное два года назад командой, которой больше нет. Приложение работает: конференции создаются, сессии показываются, участники регистрируются. Но кодовую базу никто не поддерживал. Ваша работа — исследовать её, понять и улучшить.

Стек: **ASP.NET Core 6 + EF Core 6 + React + Vite + PostgreSQL**. `docker-compose up` поднимает рабочее окружение — сделайте это до первой таски.

| Сущность | Ключевые поля |
|--------|-----------|
| **Conference** | Id, Title, Description, Location, StartDate, EndDate, Status |
| **Session** | Id, Title, Description, StartTime, EndTime, Capacity, ConferenceId, SpeakerId, RoomId |
| **Speaker** | Id, FirstName, LastName, Bio, Email |
| **Room** | Id, Name, Capacity, Location |
| **Attendee** | Id, FirstName, LastName, Email |
| **Registration** | Id, ConferenceId, AttendeeId, RegisteredAt, Status |

## Маршрут

Работа **индивидуальная**, таски идут строго по порядку — каждая опирается на предыдущие, перепрыгивать не стоит. Есть **два трека**; выбирайте по бэкенд-стеку, которым реально владеете, — вы должны быть способны *проверять* работу агента, а не просто применять её. Выбор фиксируется в первой же таске: в PR таски EXT-100 вы пишете, какой трек взяли и почему.

**Трек A — .NET.** Стабилизировать существующий бэкенд, затем строить фичи на нём.

| # | Таска | Тема | Процесс |
|---|------|-------|---------|
| 1 | [EXT-100](tasks/EXT-100-import-issues-mcp.md) | Импорт маршрута как GitHub Issues (MCP) | свободный |
| 2 | [BEVN-001](tasks/BEVN-001-codebase-mapping.md) | Карта кодовой базы | свободный |
| 3 | [BEVN-002](tasks/BEVN-002-api-documentation.md) | Документация API | свободный |
| 4 | [BEVN-003](tasks/BEVN-003-tech-debt-audit.md) | Аудит техдолга | свободный |
| 5 | [EXT-110](tasks/EXT-110-revive-ci.md) | Оживить CI на GitHub Actions | свободный |
| 6 | [BEVN-104](tasks/BEVN-104-standardize-api-responses.md) | Единый формат ответов и ошибок API | свободный |
| 7 | [BEVN-107](tasks/BEVN-107-input-validation.md) | Валидация входных данных | свободный |
| 8 | [BEVN-109](tasks/BEVN-109-transaction-boundaries.md) | Транзакции в мульти-шаговых записях | свободный |
| — | [BEVN-101](tasks/BEVN-101-sessions-page-slow.md) | *Опционально:* медленная страница сессий (N+1) | свободный |
| 9 | [BEVN-115](tasks/BEVN-115-registration-modal-state.md) | Модалка регистрации: устаревший state | свободный |
| 10 | [BEVN-202](tasks/BEVN-202-session-waitlist.md) | Session Waitlist | **spec-driven (superpowers)** |
| 11 | [BEVN-203](tasks/BEVN-203-registration-dashboard.md) | Дашборд регистраций участника | **spec-driven (superpowers)** |
| 12 | [BEVN-205](tasks/BEVN-205-e2e-suite.md) | E2e-тесты (Playwright) | **spec-driven (superpowers)** |

**Трек B — TypeScript.** То же discovery и работа с CI, затем — вместо стабилизации .NET — миграция бэкенда на TypeScript с исправлением известных дефектов по пути, и те же фичи на новом бэкенде.

| # | Таска | Тема | Процесс |
|---|------|-------|---------|
| 1 | [EXT-100](tasks/EXT-100-import-issues-mcp.md) | Импорт маршрута как GitHub Issues (MCP) | свободный |
| 2 | [BEVN-001](tasks/BEVN-001-codebase-mapping.md) | Карта кодовой базы | свободный |
| 3 | [BEVN-002](tasks/BEVN-002-api-documentation.md) | Документация API | свободный |
| 4 | [BEVN-003](tasks/BEVN-003-tech-debt-audit.md) | Аудит техдолга | свободный |
| 5 | [EXT-110](tasks/EXT-110-revive-ci.md) | Оживить CI на GitHub Actions | свободный |
| — | [BEVN-101](tasks/BEVN-101-sessions-page-slow.md) | *Опционально:* медленная страница сессий (N+1) — делать **до** миграции | свободный |
| 6 | [EXT-150](tasks/EXT-150-migrate-backend-ts.md) | Миграция бэкенда на TypeScript (NestJS) | **spec-driven (superpowers)** |
| 7 | [BEVN-115](tasks/BEVN-115-registration-modal-state.md) | Модалка регистрации: устаревший state | свободный |
| 8 | [BEVN-202](tasks/BEVN-202-session-waitlist.md) | Session Waitlist | **spec-driven (superpowers)** |
| 9 | [BEVN-203](tasks/BEVN-203-registration-dashboard.md) | Дашборд регистраций участника | **spec-driven (superpowers)** |
| 10 | [BEVN-205](tasks/BEVN-205-e2e-suite.md) | E2e-тесты (Playwright) | **spec-driven (superpowers)** |

Оба маршрута — одна вертикаль: настроить трекер (EXT-100), понять кодовую базу (Phase 0), оживить CI (EXT-110), привести поток регистрации в порядок (починить на месте — или чисто мигрировать), построить на нём две фичи и закончить e2e-тестами (BEVN-205). Опциональная BEVN-101 (производительность запросов) — необязательная, но настоятельно рекомендуемая.

**Нумерация.** Таски `BEVN-xxx` взяты **дословно** из оригинальной библиотеки BrownEvents — не удивляйтесь пропускам в номерах. Таски `EXT-xxx` добавлены для этого пилота. Дополнения к оригинальным таскам помечены блоками **«Pilot addition»** — оригинальный текст над ними не тронут.

Текст каждой таски лежит отдельным файлом в [`tasks/`](tasks/) этого репозитория. Первой таской (EXT-100) вы импортируете весь маршрут как GitHub Issues в свой рабочий репозиторий — дальше, начиная сессию, давайте агенту issue текущей таски, а не весь этот документ: агенту, как и вам, лишний контекст только мешает.

Кроме основного маршрута есть **[сайд-квесты](side-quests/README.md)** — шесть опциональных заданий для особо мотивированных, про внутренности самого процесса: знания проекта для агента, hooks, критик до мержа, свой skill, автономный прогон, цена работы. На оценку основного маршрута не влияют; у каждого написано, зачем он.

## Код

Код живёт в отдельном **приватном** репозитории-шаблоне `brown-events-pilot` — доступ выдаётся после отклика (напишите в [чат](https://t.me/+u5HQoDbqaO4yMTI6)). Получив доступ:

1. Нажмите **Use this template** → создайте **свою приватную копию**.
2. Добавьте проверяющего коллаборатором в свою копию.
3. Вся работа — в вашей копии: issues, ветки, PR-ы, отчёт.

## Инструменты

- **Консольный AI-агент** — обязателен: Claude Code, Codex CLI, Gemini CLI, Copilot CLI или Pi. Cursor, веб-чаты и плагины IDE не подходят — их сессии не попадут в отчёт (см. [codemie-analytics.md](codemie-analytics.md)).
- **Node.js 20+** — для сборки отчёта в конце.
- **Docker** — `docker-compose up --build` поднимает всё приложение (инструкции запуска — в README репозитория с кодом).

## Правила работы

Эти правила — то, что «прошивает» номер таски через всю цепочку тикет → ветка → сессии агента → коммиты → PR. Без них отчёт нечитаем, поэтому они обязательны.

1. **Одна таска — одна ветка.** Перед началом таски: `git checkout -b BEVN-001-codebase-map` (номер таски + короткий слаг). Работайте агентом только из этой ветки.
2. **Первое сообщение каждой сессии агента начинайте с номера таски:** «*BEVN-104: приведи ответы API к единому формату...*». Это название сессии в отчёте.
3. **Коммиты — с номером таски:** `docs: add architecture map (BEVN-001)`.
4. **Одна таска — один Pull Request** в вашем репозитории: из ветки таски в `main`, номер в заголовке PR, ссылка на issue таски в описании (`Closes #N`). Проверили, что Definition of Done выполнен, — мержите сами и переходите к следующей таске.

Количество сессий, попыток и уточнений **не штрафуется** — работайте как обычно. Смотреть будут на подход: как ставите задачу, как итерируете, доводите ли до результата.

## Процесс: свободный и spec-driven

У каждой таски в таблицах выше указан режим:

- **Свободный режим** (EXT-100, discovery, EXT-110, стабилизационные фиксы, BEVN-101, фронтенд-фикс) — работайте с агентом так, как вам удобно.
- **Spec-driven** (миграция EXT-150 на треке B; BEVN-202, BEVN-203 и BEVN-205 на обоих треках) — до первой строчки кода должны появиться спека (что именно строим, спорные случаи решены) и план (как, по шагам). Оба документа коммитятся в ветку таски вместе с кодом — проверяющий будет читать их до диффа. Для миграции это не формальность: миграция без плана сжигает дни.

  - В Claude Code для этого ставится плагин [superpowers](https://github.com/obra/superpowers): его поток brainstorming → спека → план → реализация делает ровно это, спека и план сохраняются в `docs/superpowers/`.
  - Если работаете в другом агенте — воспроизведите тот же поток: файлы `spec.md` и `plan.md` в PR обязательны.

**CI как гейт.** Начиная с таски, следующей за EXT-110, PR мержится только с зелёным CI. Красный пайплайн — это часть задачи, а не фон.

## Лог разработки и защита

Ведите `docs/devlog.md` — журнал работы, по несколько строк после каждой таски. Писать может и агент (поручите ему в конце сессии), и вы сами; важно, чтобы там было ваше, а не пересказ диффа:

- что делали и что получилось;
- какие решения агент принял *сам* и почему вы их приняли или переделали;
- где пришлось вмешаться руками;
- что сделали бы иначе.

Devlog — чекпоинт каждой таски: в шаблоне PR вашего репозитория есть чекбокс «devlog обновлён», и таска не считается закрытой без записи. Это не отчётность ради отчётности. Финал пилота — **защита**: созвон с тренером, где вы рассказываете о своей работе. Хороший devlog — это готовый конспект этого рассказа; без него через пару недель вы не вспомните и половины.

## Финальный отчёт

Когда все таски вашего трека смержены, соберите отчёт о вашей работе с агентом. Из папки репозитория:

```bash
npx -y -p @codemieai/code codemie analytics --report \
  --last 30d --include-external \
  --project brown-events-pilot \
  --report-format both --report-output ./report/report.html
```

`--project brown-events-pilot` — фильтр по имени папки: если клонировали репозиторий в папку с другим именем, подставьте своё. Окно `--last 30d` покрывает 14–20 дней работы с запасом; если работали дольше — укажите точные даты (`--from ... --to ...`).

Дальше:

1. **Откройте `report/report.html` в браузере** и проверьте глазами: внутри только сессии этого проекта, ничего лишнего. Что попадает в отчёт и что нет — в [codemie-analytics.md](codemie-analytics.md); коротко: метрики сессий, ветки и текст первого сообщения каждой сессии, но не полные диалоги и не содержимое файлов.
2. **Закоммитьте оба файла** (`report/report.html`, `report/report.json`) последним PR в свой репозиторий.
3. **Напишите проверяющему**, что задание готово.

## Что будет оцениваться

- Само решение: выполнен ли Definition of Done каждой таски, качество кода и документов.
- Спека и план в spec-driven тасках: полнота, решённые развилки, соответствие плана реализации.
- Трассировка: читается ли по репозиторию и отчёту цепочка «таска → сессии → коммиты → PR», зелёный ли CI на мержах после EXT-110.
- Работа с агентом по отчёту и devlog: как ставите задачи, как итерируете, замечаете ли решения, которые агент принял молча, — не число попыток.
- Защита: связный рассказ о своей работе на финальном созвоне.

## Вопросы

Если что-то не поднимается, не собирается или непонятно сформулировано — напишите проверяющему сразу, не тратьте дни на борьбу с окружением. Борьба с легаси-кодом — часть задания; борьба с инструкцией — нет.
